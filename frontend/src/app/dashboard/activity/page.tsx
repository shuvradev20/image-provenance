import { ActivityContainer } from "@/components/sections/activity/ActivityContainer";

export default function ActivityPage() {
  return (
      <div className="w-full h-full animate-in fade-in duration-300">
          <ActivityContainer/>
      </div>
    );
}